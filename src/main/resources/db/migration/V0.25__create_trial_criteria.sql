CREATE TABLE trial_criteria (
                                id BIGSERIAL PRIMARY KEY,
                                label VARCHAR(100) NOT NULL,
                                description TEXT
);